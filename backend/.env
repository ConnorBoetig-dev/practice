AWS_ACCESS_KEY_ID=AKIA...
AWS_SECRET_ACCESS_KEY=YOUR_SECRET
AWS_REGION=us-east-1

UPLOAD_BUCKET=my-app-uploads      # 👈 your S3 bucket name
PRESIGN_EXPIRY=900                # 900 s = 15 min

# 🔐 .env - Secret settings (NEVER commit this to Git!)

# URL of our API Gateway (we'll create this in AWS later)
VITE_API_BASE=https://your-api-id.execute-api.us-east-1.amazonaws.com/prod

# Name of our S3 bucket
VITE_S3_BUCKET=conlearn-uploads-bucket

# Note: In Vite, environment variables MUST start with VITE_ to be visible in the browser